let i = 0;
var saved = false;
while (!saved) {
  if (localSorage.getElementById(i) === NULL) {
    localStorage.setItem(person, 0);
    saved = true;
  }
  i++;
}

// function to calculate the result of the survey
function tabulateAnswers() {
}
